import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Cine
 * - sets up the black boxes for display purposes
 * @RobertHeiermann
 * Version 5.29
 */

public class Cine extends Display
{
    /**
     * Act - do whatever the Cine wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {
        
    } // end method act
} // end class Cine
